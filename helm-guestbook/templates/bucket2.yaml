apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: hpv-crossplane-bucket2
spec:
  forProvider:
    region: eu-central-1
  providerConfigRef:
    name: default
 # deletionPolicy: Orphan